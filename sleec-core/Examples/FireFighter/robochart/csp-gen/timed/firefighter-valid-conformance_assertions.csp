
--
-- RoboChart generator version 3.0.0.202401231330
-- Automatically generated on 30-08-2024 11:48:33
--
-- Iterated compression status: true
-- Assertions compression status: false
--

include "defs/state_timed_defs.csp"
include "defs/robochart_defs.csp"
include "defs/core_defs.csp"
include "defs/core_timed_defs.csp"
include "../../src-gen/firefighter-valid.csp"
include "defs/file_main_defs.csp"
include "defs/UAV.csp"

channel r__

-- OPERATION BOUNDS

-- MODULE CONSTANTS

-- CONTROLLER CONSTANTS

-- STATEMACHINE CONSTANTS

-- OPERATION CONSTANTS

-- BASIC TYPES
-- generate nat
nametype core_nat = { 0..2}
-- generate int
nametype core_int = { -2..36}
-- generate string
nametype core_string = LSeq(Char,2)
-- generate boolean
nametype core_boolean = Bool
-- generate real
nametype core_real = union({ -2..2},TemperatureRange) 

-- Change the value below if needed.
nametype TemperatureRange = { -2..36}
-- VECTOR TYPES AND OPERATIONS
 

-- CLOCKS
-- generate core_clock_type
nametype core_clock_type = {0..301}

-- generate const_UAV_poll
const_UAV_poll = 1

-- generate const_UAV_goHomeDelay
const_UAV_goHomeDelay = 2

-- OPERATION CONSTANTS

-- BASIC TYPES

-- generate const_UAV_init
const_UAV_init = 1

-- BASIC FUNCTIONS			
-- generate Plus
Plus(e1,e2,T) = if member(e1+e2,T) then e1+e2 else e1

-- generate Minus
Minus(e1,e2,T) = if member(e1-e2,T) then e1-e2 else e1

-- generate Mult
Mult(e1,e2,T) = if member(e1*e2,T) then e1*e2 else e1

-- generate Div
Div(e1,e2,T) = if member(e1/e2,T) then e1/e2 else e1

-- generate Modulus
Modulus(e1,e2,T) = if member(e1%e2,T) then e1%e2 else e1

-- generate Neg
Neg(e1,T) = if member(-e1,T) then -e1 else e1

-- generate vt
vt(x__,Time_seconds) = x__
vt(x__,Time_minutes) = x__*60

-- Datatype mappings

-- Specification
Timed(OneStep) {

RCheck1_spec = 
	let
		-- The external 'm' channels for every measure of FRule2
		MemoryExternalEvents = {||}
		-- The internal 'i_m' channels for every measure of FRule2
		MemoryInternalEvents = {||}
		-- Events of FRule2 except for those of measures, and any unmapped SUV events:
		CommonProvideEvents = diff(union(AFRule2,diff(UAV::sem__events,MappedRCEvents)),MemoryExternalEvents)
		-- The memory process
		Memory = TRUN(CommonProvideEvents)
		-- Ordered reading
		MemoryInOrder = SKIP
		-- Mapped events
		MappedRCEvents = {|UAV::SoundAlarm,UAV::CameraStart|}
		-- SLEEC rule renamed
		Rule = SLEECFRule2
	within
		timed_priority(
			(
				(Rule ||| TRUN(diff(CommonProvideEvents,AFRule2)))
				[| union(CommonProvideEvents,MemoryInternalEvents) |]
				(
					Memory
					[| MemoryExternalEvents |]
					MemoryInOrder
				)
			) \ MemoryInternalEvents
		)

RCheck1_suv =
	let
		-- The external 'm' channels for every measure of FRule2
		MemoryExternalEvents = {||}
		-- The internal 'i_m' channels for every measure of FRule2
		MemoryInternalEvents = {||}
		-- Events of FRule2 except for those of measures, and any unmapped SUV events:
		CommonProvideEvents = diff(union(AFRule2,diff(UAV::sem__events,MappedRCEvents)),MemoryExternalEvents)
		-- The memory process
		Memory = TRUN(CommonProvideEvents)
		-- Ordered reading
		MemoryInOrder = SKIP
		-- Mapped events
		MappedRCEvents = {|UAV::SoundAlarm,UAV::CameraStart|}
		-- SUV renamed
		SUV = ((let 
			id__ = 0
			
			
			
		within
			UAV::D__(id__)\{|UAV::terminate|})[[
		UAV::SoundAlarm.in <- SoundAlarm,UAV::SoundAlarm.out <- SoundAlarm,
		UAV::CameraStart.in <- CameraStart,UAV::CameraStart.out <- CameraStart
		]])
	within
		timed_priority(
			(
				(SUV ; STOP)
				[| union(CommonProvideEvents,MemoryInternalEvents) |]
				(
					Memory
					[| MemoryExternalEvents |]
					MemoryInOrder
				)
			) \ MemoryInternalEvents
		)

}

assert RCheck1_spec [T= RCheck1_suv

-- Datatype mappings

-- Specification
Timed(OneStep) {

RCheck2_spec = 
	let
		-- The external 'm' channels for every measure of FRule7
		MemoryExternalEvents = {|personNearby,temperature|}
		-- The internal 'i_m' channels for every measure of FRule7
		MemoryInternalEvents = {|i_personNearby,i_temperature|}
		-- Events of FRule7 except for those of measures, and any unmapped SUV events:
		CommonProvideEvents = diff(union(AFRule7,diff(UAV::sem__events,MappedRCEvents)),MemoryExternalEvents)
		-- The memory process
		Memory = (
			Mem_personNearby(CommonProvideEvents)
			[| CommonProvideEvents |] 
			Mem_temperature(CommonProvideEvents)
		)
		-- Ordered reading
		MemoryInOrder = personNearby?x__ -> temperature?x__ -> MemoryInOrder
		-- Mapped events
		MappedRCEvents = {|UAV::SoundAlarm,UAV::GoHome,UAV::temperature,UAV::BatteryCritical,UAV::personNearby|}
		-- SLEEC rule renamed
		Rule = SLEECFRule7
[[					personNearby <- i_personNearby,
					temperature <- i_temperature
]]	within
		timed_priority(
			(
				(Rule ||| TRUN(diff(CommonProvideEvents,AFRule7)))
				[| union(CommonProvideEvents,MemoryInternalEvents) |]
				(
					Memory
					[| MemoryExternalEvents |]
					MemoryInOrder
				)
			) \ MemoryInternalEvents
		)

RCheck2_suv =
	let
		-- The external 'm' channels for every measure of FRule7
		MemoryExternalEvents = {|personNearby,temperature|}
		-- The internal 'i_m' channels for every measure of FRule7
		MemoryInternalEvents = {|i_personNearby,i_temperature|}
		-- Events of FRule7 except for those of measures, and any unmapped SUV events:
		CommonProvideEvents = diff(union(AFRule7,diff(UAV::sem__events,MappedRCEvents)),MemoryExternalEvents)
		-- The memory process
		Memory = (
			Mem_personNearby(CommonProvideEvents)
			[| CommonProvideEvents |] 
			Mem_temperature(CommonProvideEvents)
		)
		-- Ordered reading
		MemoryInOrder = personNearby?x__ -> temperature?x__ -> MemoryInOrder
		-- Mapped events
		MappedRCEvents = {|UAV::SoundAlarm,UAV::GoHome,UAV::temperature,UAV::BatteryCritical,UAV::personNearby|}
		-- SUV renamed
		SUV = ((let 
			id__ = 0
			
			
			
		within
			UAV::D__(id__)\{|UAV::terminate|})[[
		UAV::temperature.in <- temperature,UAV::temperature.out <- temperature,
		UAV::BatteryCritical.in <- BatteryCritical,UAV::BatteryCritical.out <- BatteryCritical,
		UAV::SoundAlarm.in <- SoundAlarm,UAV::SoundAlarm.out <- SoundAlarm,
		UAV::GoHome.in <- GoHome,UAV::GoHome.out <- GoHome,
		UAV::personNearby.in <- personNearby,UAV::personNearby.out <- personNearby
		]])
[[				personNearby <- i_personNearby,
				temperature <- i_temperature
]]	within
		timed_priority(
			(
				(SUV ; STOP)
				[| union(CommonProvideEvents,MemoryInternalEvents) |]
				(
					Memory
					[| MemoryExternalEvents |]
					MemoryInOrder
				)
			) \ MemoryInternalEvents
		)

}

assert RCheck2_spec [T= RCheck2_suv

